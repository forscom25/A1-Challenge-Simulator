# Yongin Circuit MGeo → FSDS CSV 변환 가이드

이 문서는 대회 측에서 제공한 용인 서킷 MGeo 지도 파일(`node_set.json`,
`link_set.json`, `road_mesh_out_line.json`)의 구조와, 이를 FSDS(Formula
Student Driverless Simulator)에서 읽을 수 있는 콘(cone) CSV 트랙 파일로
변환한 방법을 정리한 문서입니다. 실제 변환 스크립트(`mgeo_to_fsds.py`)와
결과물 CSV는 이 문서 저장소가 아니라 실제 프로젝트 체크아웃의
`Formula-Student-Driverless-Simulator/maps/YonginCircuit/`에 있습니다
(이 저장소는 문서 전용이라 프로젝트 파일 자체는 포함하지 않음 — 자세한
설명은 `README.md` 참고).

원본 지도 파일 위치:
`~/git/Yongin Circuit_MGeo 1차 배포자료_260415/`

## 1. MGeo 원본 파일 구조

MGeo는 (MORAI SIM 계열에서 흔히 쓰이는) HD맵 포맷으로, 도로를
"노드(교차점/기준점)"와 "링크(차선 중심선 구간)"로 이루어진 그래프,
그리고 실제 포장 도로 영역의 외곽선 폴리곤으로 표현합니다. FSDS의
콘 트랙 포맷과는 근본적으로 다른, 훨씬 정보량이 많은 도로망 포맷입니다.

### 1.1 `node_set.json`

노드(점) 목록. 총 133개.

```json
{
  "idx": "Node_0000",
  "node_type": null,
  "point": [-423.576, 68.092, 157.427],
  "on_stop_line": false,
  "traffic_light_id": null
}
```

- `idx`: 노드 고유 ID
- `point`: `[x, y, z]` — 로컬 평면 좌표계로 보이며(작은 값, UTM 절대좌표가
  아님), 단위는 미터로 추정. `z`는 고도(해발고도, 약 136~166m 범위 확인됨)
- `node_type`, `traffic_light_id`: 이 데이터셋에서는 전부 `null` — 신호등
  등 실제 도로 표지 정보는 없음 (일반 도로가 아닌 서킷/테스트트랙이므로)
- `on_stop_line`: 전부 `false`로 확인됨 → 출발선 위치 힌트로 쓸 수 없었음

### 1.2 `link_set.json`

링크(차선 중심선 구간) 목록. 총 133개, 노드 개수와 정확히 동일.

```json
{
  "idx": "Link_0000",
  "from_node_idx": "Node_0000",
  "to_node_idx": "Node_0001",
  "points": [[-423.576, 68.092, 157.427], [-423.187, 68.407, 157.427], ...]
}
```

- `from_node_idx` / `to_node_idx`: 이 링크가 연결하는 두 노드
- `points`: 두 노드 사이를 촘촘하게 보간한 중심선 좌표 목록 (링크당 수십 개)

**중요한 확인 사항**: 노드마다 나가는 링크(outgoing link)가 정확히 1개씩만
있고, `Link_0000`의 시작 노드에서부터 `from→to`를 계속 따라가면 133개
링크를 전부 정확히 한 번씩 지나서 다시 출발 노드로 돌아옵니다. 즉 이
데이터는 **단일 폐루프(닫힌 순환 경로)** 이며, 분기나 교차로가 없는
순수 서킷 형태입니다. 이 경로를 따라간 전체 중심선(centerline) 길이는
약 **2,564m**.

### 1.3 `road_mesh_out_line.json`

실제 포장 도로 영역의 외곽선을 나타내는 폴리곤. 리스트 안에 원소가
**1개**뿐이며, 그 구조는:

```json
{
  "idx": "exterior1",
  "points": [[x, y, z], ...],       // 2097개 점, 닫힌 링(고리)
  "interiors": [
    {
      "idx": "99_1",
      "name": "other_facilities",
      "points": [[x, y, z], ...]     // 2113개 점, 닫힌 링
    }
  ]
}
```

- `points` (exterior, 바깥쪽 경계): 길이 약 **2,660m** — 중심선 길이(2,564m)와
  매우 비슷함 → 이 링이 실제로 차선의 **한쪽 가장자리**라는 강한 증거
- `interiors[0].points`: 길이 약 **3,015m**, 이름은 `"other_facilities"`
  (기타 시설물). 중심선보다 오히려 더 긺 → 차선의 반대쪽 가장자리라면
  나올 수 없는 형태. 이름까지 고려하면 **트랙 안쪽의 건물/시설물 외곽선일
  가능성이 높고, 트랙의 실제 안쪽 경계가 아님**으로 판단하여 이번
  변환에서는 사용하지 않았습니다.

즉, 이 파일에서 신뢰하고 쓸 수 있는 것은 **exterior 링 하나뿐**이고,
반대쪽 경계선은 직접 만들어내야 합니다 (2.2절 참고).

## 2. FSDS 트랙 CSV 포맷

FSDS의 `CustomMap` 레벨은 다음과 같은 헤더 없는 CSV를 읽습니다
(`docs/custom-mapping.md` 참고):

```
tag,x,y,direction,x_variance,y_variance,xy_covariance
```

- `tag`: `blue` / `yellow` / `big_orange` 중 하나
- `x`, `y`: 미터 단위 평면 좌표
- `direction`, `x_variance`, `y_variance`, `xy_covariance`: 현재 시뮬레이터
  버전에서는 사용되지 않음 → `0.0`으로 채우면 됨
- `big_orange`(출발/피니시 게이트)는 **2개 또는 4개**가 반드시 있어야
  시뮬레이터가 출발선 방향을 계산할 수 있음

## 3. 변환 로직 (`mgeo_to_fsds.py`)

### 3.1 중심선(centerline) 추출

`link_set.json`의 `from_node_idx → to_node_idx` 연결을 `Link_0000`의
시작 노드부터 따라가며 하나의 닫힌 폴리라인으로 이어붙임 (1.2절에서
확인한 대로 단일 폐루프이므로 가능).

### 3.2 바깥쪽 경계(outer edge)

`road_mesh_out_line.json`의 `exterior1.points`를 그대로 사용, 약 4m
간격으로 등간격 재샘플링(arc-length resampling).

### 3.3 안쪽 경계(inner edge) — 미러링으로 합성

`interiors` 링을 못 쓰므로, 다음과 같이 직접 만들어냄:
바깥쪽 경계의 각 점마다 **가장 가까운 중심선 점**을 찾고, 그 중심선
점을 기준으로 바깥쪽 점을 반사(mirror)시켜 반대쪽 점을 계산
(`안쪽점 = 2*중심선점 - 바깥점`). 이렇게 하면 실제 도로 폭이 구간마다
달라지는 것까지 어느 정도 반영됨. 결과로 계산된 반폭(half-width):
최소 2.25m / 평균 5.16m / 최대 14.0m — 실제 차량 시험장 도로 폭으로
납득 가능한 값.

### 3.4 blue / yellow 판정

중심선 폴리곤의 부호 있는 면적(signed area, shoelace 공식)으로 경로가
시계방향(CW)인지 반시계방향(CCW)인지 판정. FS 규칙상 진행 방향 기준
왼쪽이 blue, 오른쪽이 yellow이며, "반시계 방향 루프에서는 안쪽이
진행방향의 왼쪽" 이라는 성질을 이용해 바깥쪽/안쪽 경계 중 어느 쪽이
blue/yellow인지 결정. 이 트랙은 실제로는 CW로 확인됨.

### 3.5 출발/피니시 게이트

중심선의 시작점 근처에서 바깥쪽·안쪽 경계 점을 하나씩 뽑아 첫 번째
`big_orange` 쌍을 만들고, 진행 방향으로 3m 더 간 지점에 두 번째 쌍을
만들어 총 4개의 `big_orange` 콘을 배치 (2개짜리 게이트도 가능하지만
방향 계산이 더 명확한 4개 방식 사용).

### 3.6 좌표 재중심화

전체 좌표를 중심선 시작점이 `(0, 0)`이 되도록 평행이동.

## 4. 실제 스케일 문제와 축소 변환

`CustomMap`의 크기/용량은 **조정 불가능**함 — 지금 쓰는 건 소스가 아닌
**미리 빌드된 패키지 바이너리**이고, 아래 두 가지 한계 모두 `.pak` 안에
컴파일되어 들어있는 Blueprint 로직이라 `.ini`나 명령줄 옵션으로 바꿀 수
있는 부분이 아님. 실제로 넓히려면 Unreal Engine 소스로 FSDS를 빌드해서
(Epic 계정 필요, 용량 ~120GB, 빌드 시간 수 시간) `CustomMap` Blueprint를
직접 고치거나 새 레벨을 만들어야 함 — 이번 프로젝트에서는 의도적으로
피한 경로.

### 4.1 문제 1: 월드 렌더링/표시 범위 한계

위 3.1~3.6 방식으로 만든 **실제 축척(1:1) 버전**
(`track_yongin_fullscale.csv`)을 FSDS `CustomMap`에 로드했을 때, 차량
스폰 지점 근처의 콘 1~2개만 보이고 나머지는 전혀 렌더링되지 않는
현상을 확인함. FSDS에 기본 포함된 예시 트랙(`FormulaElectricBelgium`,
약 60m × 20m)과 비교하면, 용인 서킷은 약 850m × 900m로 **12~45배** 더
큼.

**검증**: 콘 개수를 1334개→72개로 줄여도(단, 전체 실제 크기 그대로
유지) 여전히 스폰 근처 콘만 보임 → CSV 줄 수 제한이 아니라 **월드
렌더링/표시 범위 자체의 한계**임을 확인 (`CustomMap` 레벨은 소형
FS 트랙 전용으로 제작된 것으로 추정).

### 4.2 문제 2: 콘 개수 하드 캡(~128개) — 화면과 별개로 실제 시뮬레이션 데이터도 잘림

2026-09-03에 `/testing_only/track` 토픽(화면 렌더링과 무관한, RPC로
받는 실제 콘 목록 ground truth)을 직접 조회해서 확인한 **별개의 더
근본적인 한계**. 처음 만든 축소판(blue 100개 + yellow 93개 = 193개)은
화면상으로는 멀쩡해 보였지만, `/testing_only/track`으로 실제 데이터를
조회하니 blue는 100개 중 **35개만** 존재했음 (yellow는 93개 전부
존재). 즉 `CustomMap` Blueprint는 한 가지 색을 먼저 다 채운 뒤 나머지
색을 이어서 채우는 방식으로 **하나의 공유 spline에 최대 약 128개**
(yellow 93 + blue 35 = 정확히 128)까지만 담는 것으로 확인됨 —
화면에 보이는 것과 실제 물리/센서 데이터에 존재하는 콘 목록이 다를 수
있다는 뜻이므로, 시각적으로 괜찮아 보여도 반드시 RPC로 재확인 필요.

`mgeo_to_fsds.py`는 이제 `MAX_TOTAL_CONES = 120`(여유분 포함 안전선)을
넘지 않도록 `SHAPE_SCALE`을 더 작게 잡음. 재확인 결과 blue 63개 +
yellow 57개 + big_orange 4개 = 총 124개가 `/testing_only/track`에
전부 정상적으로 나타남.

### 4.3 해결: 형태(shape)는 유지, 스케일만 축소

단순히 좌표를 균일 축소하면 콘 간격과 도로 폭까지 비현실적으로
좁아지므로 (예: 0.15배 축소 시 도로 폭이 약 1.5m로, 차량보다도 좁아짐),
다음과 같이 처리:

1. 중심선의 좌표만 `SHAPE_SCALE`배로 축소 (현재 값 0.09, 전체 루프
   길이 2,564m → 약 230m — 위 4.1 월드 범위 한계와 4.2 콘 개수 한계를
   동시에 만족시키도록 잡은 값)
2. 축소된 중심선을 기준으로, 각 점의 진행방향(tangent)에 수직인
   방향으로 고정된 반폭 `TRACK_HALF_WIDTH = 2.25m`만큼 좌우로 오프셋을
   주어 **새로 경계선을 생성** (실제 도로 폭을 미러링하는 대신, 실제
   FS 트랙 수준의 고정폭 사용)
3. 약 4m 간격으로 재샘플링, 출발/피니시 게이트는 동일한 방식으로 생성

결과물이 최종적으로 로드해서 쓰는 `track_yongin.csv` (약 77m × 46m,
콘 124개, 실제 용인 서킷과 동일한 코너 배열/형태를 유지한 축소판).
실제 크기 버전은 `track_yongin_fullscale.csv`로 참고용으로만 보관.

만약 나중에 `SHAPE_SCALE`을 키우거나 콘 간격을 더 촘촘하게 바꾼다면,
화면으로만 확인하지 말고 반드시 `/testing_only/track`을 다시 조회해서
콘 개수가 실제로 전부 존재하는지 확인할 것 (4.2절 참고).

## 5. 시뮬레이터에 불러오기

```bash
# 프로젝트 폴더 경로에 공백이 있으면 -CustomMapPath 인자가 깨지므로
# (SETUP_DEBUG_LOG.md Bug 2 참고), 공백 없는 경로로 복사 후 사용
cp track_yongin.csv /home/ailab/track_yongin.csv
cd "/home/ailab/git/A1 Challenge Simulator/simulator"
./FSDS.sh "-CustomMapPath=/home/ailab/track_yongin.csv" -windowed -ResX=1280 -ResY=720
```

또는 시뮬레이터 메인 메뉴의 Level 드롭다운에서 "CustomMap"을 선택하고
텍스트 입력창에 (공백 포함) 전체 경로를 붙여넣어도 됨 — 이 경우는
명령줄 인자가 아니라 GUI 텍스트 입력이라 공백 문제가 없음.

## 6. 알려진 한계

- **실제 축척이 아님**: 형태(코너 순서, 헤어핀/에스자 구간)만 실제와
  유사하며, 절대 거리·속도 값은 실제 서킷과 다름. 경로 추종(trajectory
  following) 로직 검증용으로는 충분하지만, MORAI SIM 등에서 실제
  스케일로 재검증이 필요함.
- **콘 개수 하드 캡 (~128개)**: 4.2절 참고. `SHAPE_SCALE`을 키우거나
  간격을 좁히면 반드시 `/testing_only/track`으로 재검증할 것 — 초과분은
  시뮬레이터가 아무 에러/경고 없이 조용히 누락시킴.
- 도로 폭은 실제 폭 변화를 반영하지 않고 4.5m로 고정.
- blue/yellow 좌우 배정은 기하학적 추정치이므로, 실제 주행 방향과
  맞는지 한 번 확인 필요 (반대라면 CSV에서 태그를 서로 바꾸면 됨).
- 고도(고저차, 실제로는 약 30m 차이)는 버려짐 — FSDS CSV 포맷 자체가
  2차원(x, y)만 지원하므로 트랙이 평평하게 투영됨.
