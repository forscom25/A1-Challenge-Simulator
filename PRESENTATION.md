# 진행상황 발표 — 실차 스케일 시뮬레이터 + 컨트롤러 스켈레톤

발표용 초안. 슬라이드 단위로 `---`로 구분해뒀음 — 필요에 따라 합치거나
쪼개서 사용. 실제 수치/스크린샷/GIF는 각 섹션에 표시된 자리에 채워 넣을 것.

---

## 1. 배경 — 왜 소스에서 다시 빌드했는가

- 패키지된 바이너리(v2.2.0)의 `CustomMap`으로 실제 용인 서킷을 테스트하려 했으나
  두 가지 근본적 한계에 부딪힘:
  - 월드/렌더 범위가 소형 FS 트랙(약 60m×20m) 기준으로 만들어져 있어 실제
    서킷(약 850m×900m) 자체가 안 들어감
  - 트랙 로딩 자체도 소형 트랙을 가정한 구조
- 결론: Unreal Engine 4.27을 **소스에서 직접 빌드**해서, 실제 스케일 그대로의
  레벨을 새로 만들기로 결정

---

## 2. 실제 스케일 맵 제작

- **MGeo → FSDS 변환**: 대회 측에서 받은 실제 용인 서킷 MGeo 데이터를
  FSDS가 쓸 수 있는 콘 좌표로 변환 (`mgeo_to_fsds.py`)
- **UE4 4.27 소스 빌드**: 과정에서 실제로 겪은 문제 두 가지
  - 여러 엔진 타겟을 한번에 병렬 빌드하면 Epic의 진행률 표시 스크립트에
    레이스 컨디션이 생겨 일부 타겟이 조용히 누락됨 → 타겟을 하나씩 빌드해서 해결
  - `AirLib`을 시스템 컴파일러로 빌드하면 UE4 자체 번들 툴체인과 glibc/ABI가
    맞지 않아 링크 실패 → UE4가 번들로 갖고 있는 clang + sysroot로 다시 빌드해서 해결
- **Python Editor Scripting으로 레벨 제작**: GUI 클릭 대신 Python으로
  스플라인에 실제 좌표를 채워서 콘 트랙을 생성 — **총 루프 길이 2563m,
  콘 1278개** (기존 소형 트랙 대비 스케일 완전히 다름)
- **검증**: 키보드 수동 조작으로 전체 랩 완주 확인

*(자리: 레벨 스크린샷 또는 에디터 뷰포트 캡처)*

---

## 3. 컨트롤러 설계 — Pure Pursuit (Python)

### 아키텍처

- ROS2 노드 2개로 분리, 토픽으로만 통신:
  - `path_publisher_node` — 참조 경로 퍼블리시
  - `controller_node` — Pure Pursuit 로직 + 제어 명령 퍼블리시
- **의도적 설계**: 이렇게 분리해두면 나중에 MPC(C++)로 교체할 때
  `controller_node`만 갈아끼우면 됨 — 인터페이스(토픽/메시지)는 그대로

### 알고리즘

- 매 오도메트리 콜백마다:
  1. 경로 상에서 현재 위치로부터 **lookahead distance**만큼 앞선 목표점 탐색
  2. 목표점을 차량 기준 좌표계로 변환 → 곡률 계산 → 조향각 산출
  3. 목표 속도(3 m/s) 대비 비례 제어로 스로틀 산출
- `fs_msgs/ControlCommand`로 퍼블리시 → FSDS 브릿지가 시뮬레이터에 그대로 전달

### 디버깅 과정에서 얻은 교훈

- 처음엔 여러 층위에서 버그가 겹쳐 있었음: 좌표축 방향 불일치, 조향 부호 반전,
  목표점 탐색 로직이 경로 이탈 시 복구 못 하는 문제, 그리고 최종적으로는
  **참조 경로 데이터 자체가 실제 트랙과 어긋나 있던 문제**
- 각 단계마다 시뮬레이터가 실시간으로 제공하는 **ground truth 토픽**과
  직접 대조해서 검증 — 정적 파일이나 캐시된 데이터를 신뢰하지 않고
  "지금 실제로 시뮬레이터가 뭐라고 말하는가"를 기준으로 삼은 것이 문제 해결의 핵심
- 결과: 참조 경로를 실시간 콘 ground truth(`/testing_only/track`)로부터
  직접 구성하도록 바꾸자 실제 트랙 중심선 대비 오차 **0.17m** 수준으로 안정적으로 추종

*(자리: 주행 GIF)*

---

## 4. 시뮬레이터가 실제로 모델링하는 것 — PhysX 차량 동역학

- FSDS 차량은 단순 운동학 모델이 아니라 Unreal의 **PhysX Vehicle 시스템**을
  그대로 사용 — 질량·관성·서스펜션·타이어 슬립·엔진/변속기까지 물리 시뮬레이션
- 확인된 주요 파라미터:
  - 질량 255kg, 공력 드래그 계수 0.3
  - 엔진: 최대 RPM 13000, 고회전형 토크 커브 (모터사이클 엔진 특성)
  - 변속기: **1단 고정** (사실상 단일 기어 직결 구동)
  - 디퍼렌셜: Open 4W, 전/후 토크 분배 0.65/0.35
  - 조향: 앞바퀴만 조향(순수 전륜조향), 최대 25° — `ControlCommand`의
    정규화 범위(±25°)와 정확히 일치
- 이런 사양 때문에 Pure Pursuit 같은 순수 기구학적 컨트롤러는 스켈레톤/기초
  검증 단계에는 적합하지만, 실제 그립 한계나 하중 이동을 반영하려면 다음
  단계(MPC)가 필요함

---

## 5. 다음 단계 — MPC(C++)로 전환

- Pure Pursuit는 목적을 달성함: 실제 스케일 맵에서 기본 인터페이스와
  제어 루프가 정상 동작함을 검증
- 다음 컨트롤러는 **MPC를 C++로 구현** 예정:
  - 이유: 위 §4에서 확인한 실제 차량 동역학적 제약(그립 한계, 하중 이동 등)을
    최적화 문제에 직접 반영 가능
  - 이미 확보한 실제 파라미터(토크 커브, 디퍼렌셜, 타이어 강성, 조향각 한계)를
    모델에 바로 활용 가능
  - 아키텍처가 이미 노드 분리되어 있어서 `controller_node`만 교체하면 됨
    (ROS2 인터페이스는 그대로 유지)

---

## 6. 요약

- 실제 스케일(2563m) 용인 서킷을 UE4 소스 빌드 + Python 레벨 제작으로 완성
- Pure Pursuit 스켈레톤이 ground truth 기준 0.17m 오차로 실제 트랙을 추종
- 차량이 실제 PhysX 물리 기반으로 시뮬레이션됨을 확인 (파라미터까지 특정)
- 다음: MPC(C++)로 최종 컨트롤러 개발
